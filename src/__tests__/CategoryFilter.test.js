// src/__tests__/CategoryFilter.test.js
import React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect'; // For additional matchers

test('clicking a category button adds a class of "selected" to the button', () => {
  // Mock data
  const categories = ['All', 'Chores', 'School'];
  const onCategoryChange = jest.fn(); // Mock function

  // Render the component
  
  ;

  // Find and click the button for 'Chores'
  

  // Assert that the button for 'Chores' has the class 'selected'
  
});
